"use strict";

const names = ["Diego", "Amanda", "Rodrigo"];
const numbers = new Array(0, 1, 2, 3, 4, 5);

console.log(names);
console.log(numbers);

console.log(names[0]);
console.log(numbers[numbers.length - 1]);
